import SwiftUI

@main
struct CareerPilotApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowStyle(.titleBar)
        .defaultSize(width: 980, height: 680)
    }
}
